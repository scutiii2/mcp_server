# Copy to .env in this same directory (chat_app/) and fill in real values.
# .env is loaded automatically by run.py via python-dotenv.

# Required for the OpenAI provider - /chat fails on that provider without it.
OPENAI_API_KEY=sk-...

# Required for the Claude provider - omit to leave it grayed out in the
# provider dropdown rather than failing when selected.
ANTHROPIC_API_KEY=sk-ant-...

# Required for the SAP AI Hub provider - all four needed together, from a
# BTP AI Core service key JSON. Requires `pip install -e ".[sap-ai-hub]"`
# too, since sap-ai-sdk-gen is an optional dependency (most setups won't
# use this provider). Omit any of these four to leave it grayed out.
AICORE_CLIENT_ID=
AICORE_CLIENT_SECRET=
AICORE_AUTH_URL=
AICORE_BASE_URL=
AICORE_RESOURCE_GROUP=default
# Comma-separated "deployment_model_name:Label" pairs, matching whatever
# models are actually deployed in YOUR BTP AI Core subaccount - there's
# no universal list the way there is for OpenAI/Anthropic's own APIs.
SAP_AI_HUB_MODELS=gpt-4o:GPT-4o (via SAP AI Core)

# Optional - defaults shown.
MCP_SERVER_URL=http://127.0.0.1:8010/mcp
OPENAI_MODEL=gpt-4o
CLAUDE_MODEL=claude-sonnet-5
FLASK_SECRET_KEY=change-me-to-something-random
FLASK_HOST=127.0.0.1
FLASK_PORT=5009
