# Optional. Sent as X-Internal-Token on every request ember_api proxies to
# ai_agent and mcp_server. Use the same value as mcp_server's and
# chat_app's secret_internal_api.env. Today neither server checks it on
# /mcp (only mcp_server's /upload does), so this is ready for when they do.
INTERNAL_API_TOKEN=
